#include <stdio.h>
#include <pthread.h>
#include <semaphore.h>

sem_t sem;
int main ( void ) 
{
    pid_t pid;

    pid = fork();

    if ( pid == 0 ) {

        
    }
    else if ( pid > 0 ) {
    }
    else

    return 0;
}
